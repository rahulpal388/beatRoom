name: "Check Linting"

on:
  push:
    branches:
      - master
      - production

jobs:
  lint-code:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: "run eslint"
        run: |
          cd backend
          npm run lint
